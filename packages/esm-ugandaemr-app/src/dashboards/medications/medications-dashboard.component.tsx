import { ExtensionSlot } from '@openmrs/esm-framework';
import React from 'react';

const MedicationsDashboard = () => {
  return <ExtensionSlot name="medications-dashboard-slot" state={{}} />;
};

export default MedicationsDashboard;
